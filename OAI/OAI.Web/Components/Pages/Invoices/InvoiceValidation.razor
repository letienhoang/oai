@page "/invoices/validation"

<PageTitle>Kiểm tra dữ liệu - OAI</PageTitle>

<section class="mb-4">
    <h1 class="fw-bold mb-1">Kiểm tra tính nhất quán</h1>
    <p class="text-secondary mb-0">
        Theo dõi các hóa đơn có lỗi tổng tiền, thuế hoặc dòng hàng.
    </p>
</section>

<section class="card border-0 shadow-sm rounded-4">
    <div class="card-body">
        <h2 class="h5 fw-bold mb-3">Validation issues</h2>
        <p class="text-secondary mb-0">
            Trang này sẽ hiển thị danh sách lỗi từ các hóa đơn cần kiểm tra.
        </p>
    </div>
</section>